<h1 id="new-header-center">Create a Recipe</h1>
<div class="well bs-component">
  <%= form_for :recipe, :url => recipes_path, :html => { :multipart => true, :class => "form-horizontal", :role => 'form' } do |f| %> 

    <% if @recipe.errors.any? %>
      <div class="alert alert-dismissible alert-danger">
        <button type="button" class="close" data-dismiss="alert">×</button>
        <h2>Oops! You forgot to fill in
          <%= pluralize(@recipe.errors.count, "field") %>:
        </h2>
        <ul>
          <% @recipe.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <fieldset>
        <div class="form-group">
          <%= f.label :name, {class: "col-sm-3 control-label"} %>
          <div class="col-sm-6">
            <%= f.text_field :name, class: "form-control", placeholder: "Your Name" %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :description, class: "col-sm-3 control-label" %>
          <div class="col-sm-6">
            <%= f.text_area :description, class: "form-control", placeholder: "What makes this recipe special?" %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label "Upload Image", class: "col-sm-3 control-label" %>
            <div class="col-sm-6">
            <%= f.file_field :image %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :prep_time, class: "col-sm-3 control-label" %>
          <div class="col-sm-6">
            <%= f.text_field :prep_time, class: "form-control", placeholder: "How long does it take to prep?" %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :cooking_time, class: "col-sm-3 control-label" %>
          <div class="col-sm-6">
            <%= f.text_field :cook_time, class: "form-control", placeholder: "How long does it take to cook?" %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :ingredients, class: "col-sm-3 control-label" %>
          <div class="col-sm-6">
            <%= f.text_area :ingredients, class: "form-control", placeholder: "Enter each ingredient separated by a comma." %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :cooking_instructions, class: "col-sm-3 control-label" %>
          <div class="col-sm-6">
            <%= f.text_area :cooking_instructions, class: "form-control", placeholder: "Enter each instruction separated by a comma." %>
          </div>
        </div>
        <div class="form-group">
          <% if user_signed_in? %>
          <div class="col-sm-offset-3 col-sm-9">
            <%= f.submit "Create Recipe", class: "btn btn-primary" %>
          <% end %>
          </div>
        </div>
    </fieldset>
  <% end %>
</div>



